# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ParentsApplication' do
  use_frameworks!

  # Pods for ParentsApplication
  pod 'ReactiveSwift', '~> 1.1'
  pod 'Realm', '~> 2.4'
  
  target 'ParentsApplicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ParentsApplicationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
